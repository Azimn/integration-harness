[project]
name = "integration-harness"
version = "0.1.0"
description = "Reuse-first integration and evaluation harness for persistent-agent architecture experiments"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
